# Continuation checkpoint — 2026-09-22 KST

Documentation task: [issue #75](https://github.com/1XP-AI/gh-runnerd/issues/75).
This is a sanitized continuation record, not completion of G01/G02 or
authorization for live execution. The live GitHub Project, issue bodies and PR
records remain authoritative if they change after this snapshot.

## Scope and exact repository state

At capture time, `origin/main` and the local base were
`40267aac9a404f267815e5372b444bc33272af47`, the merge commit for PR #90. The
only intended changes in this issue are this checkpoint and the link from
`docs/HANDOFF.md`; no implementation, CI workflow or evidence packet owned by
another issue is changed.

The current product state is still a design/experiment backlog. A green offline
check or a merged evidence packet does not establish a live Scale Set, runner,
App, credential, launchd, Docker or Lima operation.

## Goal and Project ownership

The existing G01 Goal is preserved exactly:

> Select and pin one supported Scale Set integration path and produce a reusable evidence packet for recovery at message acknowledgement, acquisition and JIT boundaries, rerunning only changed-boundary checks while keeping unchanged evidence and live gaps explicit.

The read-only Project check on 2026-09-22 recorded the following state. No G01
or G02 Agent, status or Goal field was edited by this checkpoint.

| Item | Issue state | Project status | Agent | Evidence boundary |
|---|---|---|---|---|
| G01 | OPEN | In progress | Luna max | Full live ACK/acquisition/JIT/drain gate remains open. |
| G02 | OPEN | In progress | Luna max | Enrollment evidence is recorded; production identity/persistence and startup gaps remain. |
| #71 G01h | CLOSED | Done | Luna max | Offline idle-drain slice is merged; it does not close G01. |
| #75 | OPEN | In review after this PR is created | Luna max | This documentation checkpoint only. |

The existing G01 Goal is the only G01 objective. Do not create a duplicate
native goal, close #1 because a child PR merged, or treat #71 Done as completion
of the parent gate.

## Exact PR, CI and review ledger

### PR #91 / checkpoint candidate

At the start of this correction, PR [#91](https://github.com/1XP-AI/gh-runnerd/pull/91)
was at exact candidate head
`835b824d290456f050593439b572e539c5907ebd`. Its exact-head
[Pull Request Checks run 35621852904](https://github.com/1XP-AI/gh-runnerd/actions/runs/35621852904)
passed. The Codex findings below are against that exact documentation-only head;
this correction batches their documentation disposition and requires a fresh
exact-head review after the correction push.

### Active blocker: PR #59 / G02 readiness packet

PR [#59](https://github.com/1XP-AI/gh-runnerd/pull/59) is OPEN on
`orca/g02-mac-live-readiness` at exact head
`6d304935f84ddff2f3a892c63a665e56f8077469`. Its exact-head
[Public CI run 34227722069](https://github.com/1XP-AI/gh-runnerd/actions/runs/34227722069)
completed successfully with `Go checks`; this is the last recorded check for
that head, not a fresh candidate gate under the current validation policy.

The [latest Codex review summary](https://github.com/1XP-AI/gh-runnerd/pull/59#issuecomment-5579460324)
reports completion on `6d304935f8`, but the current-head inline findings remain
unresolved. The seven current-head P1
blockers are:

- [r3957102712](https://github.com/1XP-AI/gh-runnerd/pull/59#discussion_r3957102712): reject writable ACLs on protected paths.
- [r3957558151](https://github.com/1XP-AI/gh-runnerd/pull/59#discussion_r3957558151): reject readable ACLs on the manual PEM.
- [r3957558157](https://github.com/1XP-AI/gh-runnerd/pull/59#discussion_r3957558157): reject a root execution context at bootstrap.
- [r3958134664](https://github.com/1XP-AI/gh-runnerd/pull/59#discussion_r3958134664): clear Perl startup injection variables before hashing.
- [r3958134678](https://github.com/1XP-AI/gh-runnerd/pull/59#discussion_r3958134678): protect the verified archive between hashing and extraction.
- [r3958134686](https://github.com/1XP-AI/gh-runnerd/pull/59#discussion_r3958134686): validate ownership of tracked source files.
- [r3958134693](https://github.com/1XP-AI/gh-runnerd/pull/59#discussion_r3958134693): reject executable clone-local Git configuration.

The last independent Luna max review is recorded in
[issuecomment-5585842233](https://github.com/1XP-AI/gh-runnerd/pull/59#issuecomment-5585842233): **REJECT / do not use this runbook for live execution**. It independently calls out the clone-local Git configuration, Perl startup injection, archive replacement window, tracked-source ownership and unverified staged-toolchain concerns. The prior Codex clean message for `3cb19ce7fe` is stale and does not clear `6d304935f8`; the review history, including stale/outdated findings, must be re-read after a correction.

The staged-toolchain concern is a current Luna max NO-GO, not one of the seven
Codex P1s above and not a historical record. The exact finding
[r3957565906](https://github.com/1XP-AI/gh-runnerd/pull/59#discussion_r3957565906)
states that ownership, mode, location and a self-reported `go1.26.8` version do
not prove that `G02_GO` is an approved executable; a tampered staged tool can
run native code before later artifact checks. The required boundary is an
approved digest or signature for the complete staged distribution, verified
before the first invocation. This documentation-only correction neither fixes
nor rebuts that finding, so the Luna max **REJECT** and the native live-run
block remain in force.

Next step for #59 is one owner-led correction candidate that reproduces and
resolves the current-head P1s, addresses or specifically rebuts every current
Luna max NO-GO (including the staged-toolchain finding), triages the historical
records, and then obtains focused Luna max review, a fresh exact-head Codex
review, and the applicable hosted check for that new SHA. Until those gates are
complete, do not merge the PR or run its readiness procedure on a native Mac.

### Merged G01 evidence and remaining blockers

PR [#90](https://github.com/1XP-AI/gh-runnerd/pull/90) was merged on 2026-09-21
from exact reviewed head
`3d1ae138095b90d58cf278a55cb70c7d60bd63c7`; merge commit and current
`origin/main` are
`40267aac9a404f267815e5372b444bc33272af47`. Its exact-head
[PR check 35616681203](https://github.com/1XP-AI/gh-runnerd/actions/runs/35616681203)
passed, and Codex reported no major issues for that exact head in
[issuecomment-5762791745](https://github.com/1XP-AI/gh-runnerd/pull/90#issuecomment-5762791745).
The resulting post-merge [Public CI run 35619300315](https://github.com/1XP-AI/gh-runnerd/actions/runs/35619300315)
also passed all required jobs at the merge SHA.

PR #90 pins the broker-owned provenance trust root offline. It does not close
G01. Its recorded remaining blockers are (1) a broker-authenticated workflow
input and phase/ref/commit/run binding with an explicit trust-root authority and
(2) a provider/adapter capability for an atomic owner-plus-freshness conditional
Scale Set delete, and (3) an authenticated controller handoff that delivers and
verifies the signed broker receipt before credential parsing, with reviewed root
provisioning and rotation. The [G01 live-safety follow-up](../evidence/g01-live-safety-followup.md)
retains this delivery-verification gap explicitly: broker-side attestation alone
does not clear it, and tagged controller and paired execution remain quarantined
until the controller holds the same pinned root and the handoff contract exists.
The pinned SDK exposes only unconditional deletion, so an observe-then-delete
fallback is not authorized.

PR [#72](https://github.com/1XP-AI/gh-runnerd/pull/72), the #71 offline
idle-drain slice, merged from exact head
`4d98f68361763c7f0f84ec36f87238f16df8a235` as
`e496086c6c8478007aefb74e149fe09dda0a5199`. Its
[Public CI run 35056789835](https://github.com/1XP-AI/gh-runnerd/actions/runs/35056789835)
passed all listed jobs and Codex's final exact-head result was clean in
[issuecomment-5692209270](https://github.com/1XP-AI/gh-runnerd/pull/72#issuecomment-5692209270).
The remaining live-only G01 gaps are unchanged; this merge does not authorize a
Scale Set/session/JIT/workflow/cleanup mutation.

PR [#88](https://github.com/1XP-AI/gh-runnerd/pull/88), the current full-G02
live-evidence update at this checkpoint (but not the latest G02-related artifact),
merged from exact head
`a5827dc5b776ac675df710f6c438bbdfb498edf0` as
`6d262723ca7a26704315a5e15d6ea249d4f2cbf6`. Its
[PR check 35582540806](https://github.com/1XP-AI/gh-runnerd/actions/runs/35582540806)
passed, and the exact-head Codex review completed with findings triaged. Its
verify-only enrollment, disposable-App cleanup and selected current-login
maintenance matrix are evidence for that live boundary, not proof of full G02
completion. G02 remains OPEN / In progress; production credential persistence,
distinct controller/job identity, release-signing continuity, persistent product
startup and pre-login cold-boot behavior remain unproven.

PR [#84](https://github.com/1XP-AI/gh-runnerd/pull/84) is the newer merged R1
artifact for issue [#67](https://github.com/1XP-AI/gh-runnerd/issues/67), merged
as exact commit `558b1d61563fe96b43acab3c46b70a5a8d8ee040`. It adds the offline
foreground credential, worker-isolation and fail-closed adapter evidence in
[`docs/evidence/g02-r1-foreground.md`](../evidence/g02-r1-foreground.md), and
explicitly leaves #67 OPEN; the slice does not claim live App/API/JIT evidence
or close #67, G02 #2 or G01 #1. This R1 evidence is newer than #88 but is not
full-G02 live evidence and does not change the recorded G01/G02 Project
ownership or statuses.

## Live-operation limits

This checkpoint performed no live operation. It did not dispatch a workflow,
mint or paste a credential, create or install an App, register/delete/mutate a
runner or Scale Set, touch Keychain or launchd state, change a Docker context,
alter Lima, stop a manual runner, cancel busy work, replay a workflow, or remove
unowned resources. Existing manually installed runners and host services stay
outside this documentation task.

Offline documentation and static checks are allowed. Any future live action
requires a reviewed immutable SHA, the exact selected target and bounded
resources, scoped runner-group access, explicit maintainer authorization, and a
fresh review of the changed boundary. Same-user workdirs, Keychain and launchd
do not constitute hostile-code isolation.

## Worktree and cleanup status

The read-only `git worktree list --porcelain` inventory reported two registered
worktrees: `main` and `issue-75-doc-checkpoint`. Both reported clean status at
capture time. No worktree was pruned, removed or force-deleted; no process was
killed and no broad cleanup was attempted. The issue branch remains the owned
workspace for this focused documentation PR.

## Documentation-only validation and rollback

The following checks were run for this checkpoint; no Go red/green test or full
application suite was created because the changed surface is documentation only.

```text
git diff --check
PASS (exit 0)

rg -n '\[2026-09-22\]\(handoffs/2026-09-22\.md\)' docs/HANDOFF.md && test -f docs/handoffs/2026-09-22.md
PASS (link present and target exists)

p1="$(printf '/'; printf 'Users/')"; p2="$(printf '%s' '-----BE' 'GIN')"; p3="$(printf '%s' 'gh' '[pousr]_')"; p4="$(printf '%s' 'github_' 'pat_')"; p5="$(printf '%s' 'Bear' 'er ')"; if rg -n -e "$p1" -e "$p2" -e "$p3" -e "$p4" -e "$p5" docs/HANDOFF.md docs/handoffs/2026-09-22.md; then exit 1; else exit 0; fi
PASS (no matches; exit 1 means the scan found nothing)

git worktree list --porcelain | awk '/^worktree / {count++} END {print count}'
2
```

No secrets, raw SDK errors, JIT values, personal paths or private test logs are
included. Rollback is a normal reviewed revert of the single documentation
commit after confirming the target SHA; do not reset, delete journals, replay a
workflow or alter live resources as rollback.

## Handoff sequence

1. Keep #75 In review with this focused PR and its exact validation evidence.
2. Keep #1/#2 ownership and statuses unchanged; do not mark either gate Done.
3. Treat PR #59's current-head P1 findings as blocking until fixed or explicitly
   rebutted with exact evidence and a fresh review.
4. Treat PR #90/#72/#88's merged checks as historical evidence for their exact
   SHAs, not as authorization for live execution or completion of their parent
   gates.
